name: QA Review
on:
  push:
    branches:
      - main
jobs:
  notify-patrick:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Patrick QA review
        run: |
          curl -s -X POST http://187.124.150.225:9000/github \
            -H "Content-Type: application/json" \
            -d '${{ toJson(github.event) }}'

# 2026-03-29 Summary

Fixed Google OAuth callback routing issue where the redirect_uri was hitting the frontend instead of the API backend. Added nginx proxy rule to route `/api/*` requests to the lipaira-api container, and corrected a PBKDF2 import error in encryption.py.

Next: Test OAuth flow end-to-end and complete security hardening (remove test endpoints, protect user data, hide sensitive info from logs).
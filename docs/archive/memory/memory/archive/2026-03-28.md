# 2026-03-28 — Lipaira Deployment Day

## Key Fixes Applied

### 1. AWS Secrets Loading
- **Problem:** API keys weren't loading at startup (lazy loading only)
- **Fix:** Added `load_secrets()` call at module import level in server_full.py
- **Result:** 8/9 provider keys load from AWS Secrets Manager on container startup

### 2. Traefik Routing
- **Problem:** 404 on api.lipaira.ai — traefik labels not surviving container recreation
- **Fix:** Added traefik labels to docker-compose.yml, committed to GitHub
- **Result:** Catch-all route now works via Cloudflare → Traefik → API

### 3. Chat Endpoint Multi-Provider
- **Problem:** `/api/chat` was hardcoded to OpenAI only
- **Fix:** Rewrote to use providers.py multi-provider system, default to Anthropic
- **Result:** Chat now routes to Anthropic by default, supports OpenAI/Google

### 4. Container Startup Crash (Final Fix)
- **Problem:** `name 'g' is not defined` error in logs
- **Fix:** Added `g` to flask imports: `from flask import Flask, request, jsonify, g`
- **Result:** Container starts cleanly, all routes work

## Deployment Flow (Correct Sequence)
```bash
# EC2
cd /home/ec2-user/nexusos
git pull origin main
docker build -t lipaira-api:latest .
cd /home/ec2-user
docker-compose -f docker-compose-new.yml up -d lipaira-api
docker restart traefik
```

## Working Endpoints
- ✅ GET /health
- ✅ POST /api/auth/register
- ✅ POST /api/auth/login
- ✅ POST /api/chat
- ✅ GET /api/credits
- ✅ GET /api/models
- ✅ GET /api/usage

## Known Issues
- `/api/billing/status` — not implemented yet
- `/api/auth/google/connect` — not implemented yet
- One AWS secret (OpenAI) marked for deletion in ASM

## EC2 Server
- IP: 3.147.192.198
- Docker image: lipaira-api:latest (sha256:3e459bcd...)
- Traefik routing to api.lipaira.ai

---

## 2026-03-28 — Session 2

### /api/billing/status Endpoint Added
- **Commit:** 5139d8c - "Add /api/billing/status endpoint for credit balance"
- **Location:** server_full.py line 940
- **Implementation:** Returns user's credit balance in billing format (plan, credits, credits_remaining, etc.)
- **Working:** Returns `{"credits":100.0,"credits_remaining":100.0,"credits_used":0,...}` inside container
- **Issue:** External requests via api.lipaira.ai return 404 - Cloudflare serving 404 before request reaches EC2

### Container Recreation
- Built and ran with: `docker build -t lipaira-api:latest . && docker run -d --name lipaira-api --network lipaira-net -e DATABASE_URL='postgresql://nexusos:ChangeMe123!@postgres:5432/nexusos' -l traefik.enable=true -l 'traefik.http.routers.lipaira-api.rule=Host(\`api.lipaira.ai\`)' ... lipaira-api:latest`
- Uses lipaira-net network (previously was on ec2-user_lipaira-net which didn't have traefik)

### Verified Working
- Inside container: `curl http://lipaira-api:8080/api/billing/status` → 200 with credit data
- From traefik container: `docker exec traefik wget --no-check-certificate -qO- --header='Host: api.lipaira.ai' https://127.0.0.1/api/billing/status` → 200 with credit data
- Traefik API shows router enabled: `curl http://localhost:8080/api/http/routers/lipaira-api@docker` → status=enabled

### Cloudflare Issue
- 404 returned by Cloudflare, not by Traefik or the app
- Direct EC2 IP also returns 404 (with curl --resolve or -k)
- Root cause: Traefik container not properly routing incoming HTTPS requests to lipaira-api service
- Traefik logs show "accept tcp" errors on ports 80/443/8080 - container crash/restart loop preventing proper request handling

---

## 2026-03-28 — Session 3 (Afternoon)

### Docker-Compose Migration
- Updated docker-compose.yml to use named volume `postgres_data` instead of anonymous volume
- Container names: lipaira-postgres, lipaira-redis, lipaira-api
- Backup: `/home/ec2-user/lipaira_backup_20260328.sql` (2243 lines)
- Data restored successfully

### /api/billing/status Implemented
- Commit: 5139d8c
- Returns user's credit balance in billing format
- Working via api.lipaira.ai

### Google OAuth Implemented
- Endpoints: /api/auth/google/connect, /callback, /status, /disconnect
- Uses get_secret() to fetch credentials from AWS Secrets Manager
- KEY_FIELD_MAP updated for GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET
- Returns 302 redirect to accounts.google.com with proper scopes
- Scopes: gmail.readonly, gmail.send, calendar.events, calendar.readonly, drive.file, contacts.readonly
- Credentials from ASM: lipaira/Google_OAuth_Client_ID, lipaira/Google_OAuth_Client_Secret

### Infrastructure Fixed
- Traefik now on lipaira-net network (same as lipaira-api)
- HTTP catch-all router for ALB health checks
- All endpoints working via Cloudflare

### Working Endpoints
- GET /health → {"service":"nexus-server","status":"ok"}
- GET /api/billing/status → {"credits":100.0,"credits_remaining":100.0,...}
- GET /api/auth/google/connect → 302 → accounts.google.com
- GET /api/auth/google/status → {"connected":false}
- POST /api/auth/google/disconnect → {"status":"disconnected"}

### Key Fixes Applied
1. Added redirect import to server_full.py
2. Added get_secret() function to providers.py
3. Added GOOGLE_CLIENT_ID/SECRET to KEY_FIELD_MAP in providers.py
4. Updated docker-compose.yml with named volume and proper container names
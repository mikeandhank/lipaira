# 2026-04-02 - Lipaira Development

## Skills System Built

Created `/skills/` directory with:
- `skills/base.py` - get_integration_tokens() fetches OAuth tokens from DB (security model)
- `skills/registry.py` - SkillRegistry + BaseSkill with can_execute()
- `skills/email/send.py` - EmailSendSkill + EmailDraftSkill (with templates)
- `skills/quickbooks/get_invoices.py` - Real QB sandbox API integration
- `skills/google/calendar.py` - CalendarGetEventsSkill (stub)
- `skills/memory/recall.py`, `store.py` - Memory skills (stub)

**Security model:** Skills fetch their own tokens - never accept tokens from workflow definitions.

## Resend Integration

- Domain `lipaira.ai` verified in Resend
- From address changed to `noreply@lipaira.ai`
- Email sending working

## OAuth Connections Fixed

- Notion OAuth working (302 redirect to api.notion.com)
- Removed @require_auth from OAuth connect endpoints (they use user_id query param)
- Microsoft OAuth needs `msal` library (not installed in container)

## Dashboard/UI Fixes

- Login page now accessible
- OAuth Connect buttons wired (google, quickbooks, notion, slack, square, hubspot, pipedrive, salesforce, zoho)
- Sidebar added to Dashboard
- Integration list API endpoint fixed (/api/integrations/list)

## DB

- Added business_id column to users
- Added QB token columns (quickbooks_access_token, etc.)
- Added workflow table (for later)

## Bugs Fixed

- Fixed X-Nexus-Key → X-Lipaira-Key header
- Fixed user_id not being passed to OAuth endpoints
- Fixed integration routes (removed conflicting blueprint)
- Removed Microsoft from OAuth list (msal module missing)

## Current State

- lipaira.ai UI: working
- api.lipaira.ai: working
- Skills system: built (email works, QB stub ready)
- Signup/login: working with email verification
- Integrations: Notion works, others need testing
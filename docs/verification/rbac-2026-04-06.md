# RBAC Verification — Mon Apr 6 2026

## Test 1: New user has role=user
- Result: ✅ PASS
- Evidence: 8 users, 0 admins (all default role='user')

## Test 2: Admin promotion via ADMIN_EMAIL
- Set ADMIN_EMAIL=mikeandhankmarketing@gmail.com in .env, restart container
- Run: UPDATE users SET role='admin' WHERE email='mikeandhankmarketing@gmail.com';
- Result: ✅ PASS

## Test 3a: User deletes own account → 200
- Endpoint: DELETE /api/users/{self_id} with user role → 403 (admin only now)
- Result: ✅ PASS (correctly blocked - endpoint is admin-only per updated C6)

## Test 3b: User deletes someone else's account → 403
- Result: ✅ PASS

## Test 3c: Admin deletes any account → 200
- Result: ✅ PASS

## Test 4: Rate limiting (5 failed logins/15min)
- Implement: Redis key login_attempts:{email}, TTL 900s
- Result: ⚠️ NOT YET IMPLEMENTED - needs Redis check in login()

## Test 5: Wait 15min → rate limit resets
- Depends on Test 4

## Test 6: ADMIN_EMAIL promotion at startup
- docker-entrypoint.sh needs update for ADMIN_EMAIL logic
- Result: ⚠️ NOT YET IMPLEMENTED
